; project configuration (do not delete)

(compiler

    (alias "gcc/amigaos4/latest")

    (makefile "makefile.gcc")

    (make "make -f %makefile %target")

    (target "")

    (executable "Langages:Sources/AmiSoundED/AmiSoundED/AmiSoundED")

    (arguments "")
)
